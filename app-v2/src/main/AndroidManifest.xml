<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yan.valkyrietool.v2">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!--bugly-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme" >

        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.TestActivity"
            android:exported="true"/>
        <activity android:name=".ui.activity.ArrivalDetailActivity" />
        <activity android:name=".ui.activity.ArrivalTeamActivity" />
        <activity android:name=".ui.activity.AddTeamActivity" />
        <activity android:name=".ui.activity.CharacterDetailActivity" />
        <activity android:name=".ui.activity.SettingActivity" />
        <activity android:name=".ui.activity.ConfigCharactersActivity" />
        <activity android:name=".ui.activity.PersonalCharactersActivity" />
    </application>
</manifest>
